import {
  "moonbitlang/core/buffer",
}

import {
  "pkdxtools/pkdx/src/db" @db,
  "moonbitlang/x/sys" @sys,
  "moonbitlang/core/debug" @debug,
} for "test"

supported_targets = "native"

options(
  "native-stub": [ "process_ffi.c" ],
)
